{
  "name": "mcp-server-hub",
  "version": "1.0.0",
  "description": "🎯 开箱即用的 MCP 服务器全家桶",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean",
    "changeset": "changeset",
    "version": "changeset version",
    "publish": "pnpm -r publish --access public"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "mcp-server",
    "ai",
    "llm",
    "claude",
    "tools"
  ],
  "author": "cosmicdk",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0",
    "typescript": "^5.4.0"
  }
}
